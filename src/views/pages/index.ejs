<!doctype html>
<html lang="it">
  <%- include("../partials/head") %>
  <body class="bg-primary">
    <header
      class="w-full border-b border-b-white/5 bg-white/10 backdrop-blur-md fixed top-0 z-10"
    >
      <nav class="h-12 flex items-center justify-between container">
        <div>
          <a href="/" class="text-brand-text font-medium text-lg tracking-wide"
            >Tobacconist</a
          >
        </div>
      </nav>
    </header>
    <main>
      <section
        style="
          background: radial-gradient(
            ellipse 80% 50% at 50% -20%,
            rgba(120, 119, 198, 0.3),
            transparent
          );
        "
      >
        <div class="grid grid-cols-12 gap-x-2 container">
          <div class="col-start-3 col-end-11 mt-40">
            <div class="space-y-6 text-center text-prety text-brand-text">
              <h1 class="text-7xl font-semibold tracking-tight">
                Dati realtime per le tabaccherie
              </h1>
              <p>
                Le tue applicazioni aggiornate con prodotti, prezzi e codice a
                barre di sigarette, tabacchi a combustione, gratta e vinci e
                altri articoli delle tabaccherie
              </p>
              <div>
                <a
                  href="#"
                  class="text-brand-text bg-brand-bg px-4 py-2 h-12 text-sm font-medium inline-flex items-center justify-center rounded-full hover:brightness-125 transition-all ease-out duration-200"
                  >Contattaci</a
                >
              </div>
            </div>
          </div>
        </div>

        <div
          class="grid grid-cols-12 gap-x-2 py-32 relative before:content-[''] before:w-full before:h-full before:bg-radial before:absolute before:blur-md"
        >
          <div
            class="col-start-4 col-end-10 relative border-white/5 rounded-lg overflow-hidden before:absolute before:content-[''] before:w-full before:h-full before:inset-0 before:bg-conic before:blur-md before:animate-spin-slow after:content-[''] after:absolute after:w-full after:h-full after:-inset-px p-px"
          >
            <table
              class="border border-collapse w-full text-brand-text relative"
            >
              <thead
                class="text-sm font-medium border-b border-white/5 bg-table-header"
              >
                <tr>
                  <th class="text-left p-4 border-x border-white/5">Nome</th>
                  <th class="text-left p-4 border-x border-white/5">
                    Categoria
                  </th>
                  <th class="text-left p-4 border-x border-white/5">Status</th>
                  <th class="text-left p-4 border-x border-white/5">
                    Ultimo aggiornamento
                  </th>
                </tr>
              </thead>
              <tbody class="text-xs bg-table-row">
                <% products.forEach(p => { %>
                <tr class="border-b border-white/5">
                  <td class="text-left p-4 border-x border-white/5">
                    <%= p.name %>
                  </td>
                  <td class="text-left p-4 border-x border-white/5">
                    <%= p.groupDescription %>
                  </td>
                  <td class="text-left p-4 border-x border-white/5">
                    <%= p.active %>
                  </td>
                  <td class="text-left p-4 border-x border-white/5">
                    <%= p.updatedAt %>
                  </td>
                </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      </section>
    </main>
    <footer class="border-t border-white/5 bg-primary">
      <div class="container h-12 flex items-center">
        <a href="#" class="text-brand-text font-medium text-sm tracking-wide"
          >Tobacconist</a
        >
      </div>
    </footer>
  </body>
</html>
